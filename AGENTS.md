# AGENTS.md

このファイルは毎リクエスト課金される。**hook で機械的に強制できることは書かない。**
書くのは「hook にできないこと」だけ。100 行以内を維持する。

## コマンド

テスト・lint・build は必ずラッパを使う (生で叩くと hook が止める)。

| やること | コマンド |
|---|---|
| テスト | `agent test [-- <extra>]` |
| lint | `agent lint` |
| 型チェック | `agent typecheck` |
| ビルド | `agent build` |
| 出力全文を見る | `agent log <id> --grep <re>` |
| コンテキスト実測 | `agent stats` |
| キャッシュ残り | `agent age` |
| 安いモデルへ委譲 | `agent delegate [--write] "<task>"` |

出力は常に 40 行以内に丸められ、全文は `.agent/runs/<id>.log` にある。
**要約に欲しい情報が無いときは、コマンドを生で叩き直すのではなく `agent log` で取りに行く。**

## コンテキストの扱い

加重コストの 62% は cache_read、つまり**コンテキストに残っているトークン × 残りターン数**。
ゴミを 1 回入れると、以後の全ターンで課金され続ける。

蓄積するトークンの実効単価は **30〜60x**（生成時 + 以後の全ターンの cache_read）。
一方 **thinking は蓄積しないので 5x**。そして **1 往復 = 約 34,759 加重トークン**
（平均 ctx 347k のとき）。トークンを削るより**往復を増やさない**方がはるかに重要:

1. **`ctx` が 250k を超えたら `.agent/state.md` を更新して `/clear`。**
   実測で 3〜5 ターンで元が取れる。ステータスラインが赤くなったら合図。
   **自動圧縮は使わない。** 引き継ぎがあるなら `/clear` の方が素直で確実。
2. **同じファイルを二度読まない。** 内容はまだコンテキストにある。範囲が要るときは
   `Read(offset/limit)` か `Grep`。編集直後の確認目的の再 Read もしない
   (Edit が失敗すればエラーが返る)。
3. ファイルの位置を探すときは Read ではなく `Grep` → 当たった行の周辺だけ範囲読み。
4. **既存ファイルを Write で丸ごと書き直さない。** Edit で差分を当てる。
   （ただし新規ファイルの Write は本質的なコストなので気にしない）
5. **Bash は回数を減らす。** 1 件あたりは小さくても、回数（実測 1,554 件）が効く。
   関連する確認は 1 コマンドにまとめる。

**thinking と説明の長さは気にしなくてよい**（加重の 7.9% と 2.4%、しかも thinking は
蓄積せず 5x）。明快さや思考の深さを削っても得られるものがない。
**`old_string` の切り詰めも不要**（Edit payload の 24% で、全体では 3.5% にすぎない）。

## 委譲の判断

- **機械的で検証可能なタスク** (一括置換、ログ解析、全件列挙、雛形生成) → `agent delegate`。
  Claude のレート制限を消費しない。編集させたら必ず `agent test` と diff で検証する。
- **サブエージェント**は原則使わない。コールドスタートで約 100〜150k 加重を先払いする。
  例外は「答えは小さいが探索過程が青天井 (30k tok 超)」かつ「セッション序盤」のときだけ。
- 出力を全部使う作業 (読んで書き換える) は常にメイン 1 本が安い。

## 引き継ぎ

- 作業の区切りごとに `.agent/state.md` を**全文上書き**で更新する (追記しない・150 行以内)。
- 詳細な経緯は `.agent/journal/YYYY-MM.md` に追記する。**このファイルは読まない前提**。
- 定着した知識 (ドメイン知識・罠・方針) だけを `docs/` に昇格させる。

## ドキュメント

`docs/INDEX.md` が目次。必要になったものだけを読む。

- `docs/decisions/` — ADR。Why / Why not と踏んだ罠。古いものは削除せず `Status: superseded by ...` にする
- `docs/domain/` — ドメイン知識
- `docs/guides/` — 手順

## ファイル編集

`sed -i` / `perl -pi` / heredoc でのソース書き換えは禁止 (差分が見えず、フォーマッタも走らないため)。
3 ファイル以上の機械的一括置換など例外時のみ、理由を 1 行述べたうえで `# ALLOW-SCRIPT-EDIT` を付ける。
